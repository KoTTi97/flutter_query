## What this changes

<!-- One paragraph. What behaviour is different afterwards? -->

## How you know it works

<!--
The gate, and anything beyond it:

- [ ] `dart test` in packages/query_kit
- [ ] `flutter test` in packages/query_kit_flutter
- [ ] `flutter test` in examples/showcase and examples/sensor_demo
- [ ] `dart analyze --fatal-infos` and `dart format --set-exit-if-changed`

If this fixes a defect, say whether you reproduced it first and what the
reproduction was — a failing test in the diff is the best answer.
-->

## Fidelity

<!--
Only if this touches ported behaviour:

- which upstream file the behaviour comes from,
- whether a ported assertion changed, and
- the PORTING_NOTES entry recording it.

Delete this section if it does not apply.
-->
