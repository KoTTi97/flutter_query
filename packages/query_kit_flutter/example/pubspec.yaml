name: query_kit_flutter_example
description: A runnable, one-file tour of the binding's call styles.
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  query_kit_flutter: ^1.0.0

dev_dependencies:
  lints: '>=5.1.1 <7.0.0'
