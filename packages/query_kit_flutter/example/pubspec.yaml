name: query_kit_flutter_example
description: A runnable, one-file tour of the binding's call styles.
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  query_kit_flutter: ^0.1.0
