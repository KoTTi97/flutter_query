name: tanstack_query_flutter
description: >-
  Flutter binding for tanstack_query_core: provider, controllers, builders,
  State mixin, context.query. An independent community port, not affiliated
  with or endorsed by TanStack.
version: 0.1.0-dev
repository: https://github.com/KoTTi97/flutter_query
issue_tracker: https://github.com/KoTTi97/flutter_query/issues
topics:
  - cache
  - state-management
  - query
  - tanstack-query
  - async

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  # For `lib/testing.dart` only — a test file imports it, an app never does,
  # and both are SDK packages, so nothing is pulled from pub.dev.
  flutter_test:
    sdk: flutter
  meta: ^1.15.0
  tanstack_query_core: ^0.1.0-dev

dev_dependencies:
  clock: ^1.1.1
  lints: '>=5.1.1 <7.0.0'
