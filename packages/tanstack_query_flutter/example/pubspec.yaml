name: tanstack_query_flutter_example
description: A runnable, one-file tour of the binding's call styles.
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  tanstack_query_flutter: ^0.1.0-dev
