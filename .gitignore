query
# react-demo is vendored (its own .git was removed); node_modules is
# ignored via react-demo/.gitignore and the rule below.
node_modules
.DS_Store

flutter-port/**/.dart_tool/
flutter-port/**/build/
