name: query_core
description: A faithful Dart port of TanStack Query's query-core — cache, query state machine, observers, retries and mutations, with no Flutter dependency.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  clock: ^1.1.2
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  fake_async: ^1.3.3
  lints: ^6.0.0
  test: ^1.25.0
