name: _flutter_query_workspace
publish_to: none

environment:
  sdk: ^3.11.0

workspace:
  - packages/query_core
  # Added as each milestone creates them:
  # - packages/flutter_query   (M7)
  # - apps/sensor_demo         (M8)
